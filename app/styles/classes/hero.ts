/** Horizontal rules in the home hero (uses fg for shadow tint). */
export const heroDividerLineClass =
	"h-px w-screen bg-border-strong/80 opacity-0 shadow-[0_0_12px_rgb(var(--color-fg)/0.06)] motion-safe:animate-hero-rise motion-reduce:animate-none motion-reduce:opacity-100 dark:bg-fg-muted/50 dark:shadow-[0_0_16px_rgb(var(--color-fg)/0.05)]";

/** No solid fill so the home page particle canvas stays visible when scrolling. */
export const homeStackSectionClass = "relative z-10 scroll-mt-24";
